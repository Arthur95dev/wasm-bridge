{
  "name": "wasm-bridge",
  "version": "0.1.0",
  "description": "Typed WebAssembly bridge for TypeScript and JavaScript",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "dist/wasm-modules"
  ],
  "scripts": {
    "build": "tsc && tsc && node scripts/copy-wasm.js",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
